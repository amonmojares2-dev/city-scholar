# Email delivery through Gmail SMTP (configure real values in Render; never commit secrets)
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_PASSWORD=your_gmail_app_password
# Optional; when omitted or blank, the backend uses EMAIL_USER as the sender.
EMAIL_FROM=City Scholar <your_gmail_address@gmail.com>
